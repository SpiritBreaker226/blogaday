<section class="comments">
	<h2 class="hide">Comments For Post <%= @post.title %></h2>

	<% @comments.each do |comment| %>
		<p>
			<%= comment.body %>
		</p>
	<% end %>

	<%= paginate @comments %>
</section>
