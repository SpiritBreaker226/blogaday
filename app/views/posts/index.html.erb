<ul class="posts">
	<% @posts.each do |post| %>
		<li class="card">
			<%= link_to(post_path(post)) do %>
				<h3><%= post.title %></h3>
			<% end %>

			<%= post.get_frist_sentence %>
		</li>
	<% end %>
</ul>
